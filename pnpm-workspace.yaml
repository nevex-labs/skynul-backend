packages:
  - '.'

# pnpm v10 blocks dependency lifecycle scripts by default.
# Whitelist the deps that must run install scripts (native/binary).
onlyBuiltDependencies:
  - '@biomejs/biome'
  - esbuild
  - puppeteer
